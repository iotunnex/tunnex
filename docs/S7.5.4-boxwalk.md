# S7.5.4 per-user + temporary grants — box-walk (live, founder-present)

Two-gateway VM, **enterprise** build of S7.5.4. Netns spokes (the S7.5.3 walk shape). Evidence
committed IN-SESSION to `walk-artifacts/S7.5.4/`; scratch WG configs gitignored at creation (`scratch/`).

## Env (Leg 0)

| Thing | Value |
|---|---|
| Box / edition / migration | `<fill>` / enterprise / 30 |
| Org (`$ORG`) | `<fill>` (zero_trust_mode=enforcing) |
| Gateway A (hosts the user's device) | `<fill>` |
| Gateway B (non-hosting — carries the /32 as a group DST; the Leg-3b discriminator) | `<fill>` |
| Member subject (`$USER`) | `<fill>` (a current active member — the per-user grant subject) |
| Owner session | held on box only |

---

## Leg 1 — per-user grant on the wire + F1 org-wide strip (incl. non-hosting gateway B)  · PENDING

## Leg 2 — temporary lapse → swept + audited on the wire (grant_expired class) + re-grant-on-wire  · PENDING

## Leg 3 — downtime lapse (stop API → lapse → restart → swept + audited)  · PENDING

## Leg 4 — extend: BOTH halves (zero nft churn + the extension TOOK, ruleset byte-stable)  · PENDING

---

## Disposition

_Filled at walk end: all legs PASS → both-green → merge-time protection presence check → merge on
Pawan's explicit in-session word._
