# S14.10 — Devices Redesign Decisions & Render Floor Audit

This document records the design decisions, render-floor classification, schema invariants, and verification census for the **S14.10 Devices** redesign pass.

---

## 1. Four-Way Render Floor Audit

Every component and panel on `/devices` is audited and classified under the four-way test:

| Component / Panel | Verdict | Rationale |
|---|---|---|
| **Header & Title (`<h1 ...>`, Subtitle)** | **(a) Functional render floor** | Title MUST be `<h1 style="...">Devices</h1>` so `getByRole("heading", { name: "Devices" })` and Playwright/A11y tests resolve it. Subtitle renders `{org.name}`. Static ad-hoc `control plane ● healthy` text is removed to avoid duplicate-render desync with shell footer `HealthStatus`. |
| **Error Banner (`ErrorText`)** | **(a) Functional render floor** | Surfacing API errors (`loadDevices` failure, creation error, revocation error) is mandatory (D1(b) / `loadOne` law). Failure must render error/retry text, never a silent empty state. |
| **Gateways Pointer Banner** | **(b) Visual advisory** | Glassmorphism card linking to Gateways screen (`/gateways`). S14.6 D2 reference link. |
| **Create Device / Export Profile Form Card** | **(a) Functional render floor** | Interactive form (`New device name` input, transport selector for OpenVPN when `ovpn_enabled`, `Full tunnel` checkbox, submit button). |
| **OneTimeSecretModal Issuance Ceremony** | **(a) Functional render floor** | One-time export secret modal (`.conf` / `.ovpn`), download button, QR code (WireGuard static export only), and `pendingExport` warning callout. |
| **Devices DataTable (`caption="Devices"`)** | **(a) Functional render floor** | Table with 5 canonical column headers (`Device`, `Address`, `State`, `Posture`, `Actions`). |
| **- Device Name Cell** | **(a) Functional render floor** | Renders `d.name` text. |
| **- Address Cell** | **(a) Functional render floor** | Renders `d.assigned_ip` in `JetBrains Mono`. |
| **- State Cell** | **(a) Functional render floor** | Renders `<Badge tone="danger">revoked</Badge>` for revoked, `<Badge tone="amber">pending</Badge>` for pending, or `StatusDot` + `lastSeen(...)` for active. |
| **- Posture Cell** | **(a) Functional render floor** | Renders `postureBadge` label + `re-export needed` warning badge when `needs_reexport` is true. Suppressed on revoked rows. |
| **- Actions Cell** | **(a) Functional render floor** | Renders `Revoke` button for active devices. Suppressed on revoked/pending rows. |

---

## 2. Status Representation (`active` | `revoked` | `pending`), `health_blocked`, and `approved_by`

- **`status` Enum (`active`, `revoked`, `pending`):**
  - `active`: Normal functioning device. Renders `StatusDot` (on/off) and `lastSeen(...)`. Shows `Revoke` button.
  - `revoked`: Device revoked. Renders `<Badge tone="danger">revoked</Badge>`. Posture badges, `re-export needed`, and actions are **SUPPRESSED** via `d.status !== "revoked"`.
  - `pending`: Device awaiting admin approval under Enterprise device approval mode. Renders `<Badge tone="amber">pending</Badge>`. During issuance ceremony, `pendingExport` surfaces: *"This device is pending approval — the profile is valid but won't connect until an admin approves the device."*
- **`health_blocked: true`:**
  - Enforcement fact: Device is excluded from every gateway while `true`. When `health_state === "noncompliant"` and `health_blocked === true`, renders `"posture blocked"`.
- **`approved_by: null` (Grandfathered):**
  - `approved_by: null` indicates a grandfathered / auto-approved device enrolled when device approval mode was off. Functions as active without requiring device approval queue action.

---

## 3. Posture States & Disk Encryption Tri-State

- **`health_state` (`compliant`, `noncompliant`, `unknown`, `undefined`):**
  - `undefined` (absent): Org has no posture checks configured. `postureBadge` returns `null` (no posture noise / no badge).
  - `health_blocked: true`: Returns `{ label: "posture blocked", tone: "danger" }`.
  - `noncompliant`: Returns `{ label: "posture warning", tone: "warn" }`.
  - `compliant`: Returns `{ label: "posture ok", tone: "ok" }`.
  - `unknown`: Returns `{ label: d.health_reported_at ? "posture stale" : "posture not reported", tone: "unknown" }`. **`unknown` MUST NEVER render as a pass** (absence is not compliance).
- **`health_disk_encrypted` Tri-State (`true`, `false`, `undefined`):**
  - `true` $\rightarrow$ `"encrypted"`
  - `false` $\rightarrow$ `"not encrypted"`
  - `undefined` $\rightarrow$ `"not reported"` (never an em-dash or guessed value).

---

## 4. Control Plane Status Header Pattern Audit

- `HealthStatus` (`control plane operational` / `control plane unreachable`) already exists in `AppShell.tsx` (line 322) and renders in the footer of EVERY page after querying `/healthz`.
- Hardcoding static text `control plane ● healthy` in individual page title headers (`Access.tsx`, `Devices.tsx`) is a **NEW, ad-hoc pattern** that multiplies the registered duplicate-render defect across screens and desyncs from live `/healthz` telemetry.
- **Rule:** Page headers MUST NOT hardcode static `control plane ● healthy`. Page titles render `<h1 ...>Devices</h1>` with org subtitle `{org.name}`. Shell-level footer owns control plane health.

---

## 5. `CUT-REGISTER.md` Grep Citation

Output of `grep -i 'device' docs/CUT-REGISTER.md`:
```
| **Failed-load triad panel** (Gateways/Sites right column) | **CUT** | a wireframe DOCUMENTATION device showing three states side by side, not a product panel | S14.5 |
| **`PEERS` column** (Gateways) | **ABSENT — its own slice** | `devices WHERE node_id` counts DEVICES, and a hub's WireGuard peers include SITE LINKS... | S14.6 |
- **`126 devices` on `PUSHED TO` — CUT, not served.** Same class as the gateway `PEERS` count. Counting devices locally would count ones that never fetched this list.
| **Page suites render without a Router** | **NOT A LIVE DEFECT — loud, not silent** | measured: 0 of 7 pages use useNavigate/Link/useLocation... And the first <Link> added (Devices, S14.6) crashed five tests immediately. | S14.6 |
```

Output of `grep -i 'date-range' docs/CUT-REGISTER.md`:
```
| **Date-range picker on screens that do not filter by date** | **CUT** | keep it only where the data is time-ranged: Access Events, Audit Log | EPIC 14 open |
```

---

## 6. Em-Dash Prediction & Devices Blast Radius Census

- **Em-Dash Prediction:** Verified on `accesswiring.test.tsx:103` (`"Policy not enforced — open mesh."`) and `:144` (`"Rule status unavailable — refresh."`). Replacing `—` with `-` breaks exact text matching.
- **`deviceswiring.test.tsx` Blast Radius Census:**
  1. `screen.getByRole("table", { name: "Devices" })` (DataTable caption must be `"Devices"`).
  2. Column headers: `Device`, `Address`, `State`, `Posture`, `Actions`.
  3. Revoked device row suppression: `within(rowFor("old-laptop")).queryByText("posture blocked")` must be `null`, `queryByText("re-export needed")` must be `null`. Active device `work-laptop` must contain both.
  4. Exact row count census: `expect(within(table).getAllByRole("row")).toHaveLength(3)` (1 header + 2 data rows).
  5. Failure path: `screen.getByText("Could not load devices.")`.
  6. Empty path: `screen.getByText("No devices yet.")`.

---

## 7. Verification Plan (Deliberate Red & Exact-Count Census Edit)

- **Deliberate Red per New Claim:**
  1. Changing `<h1>` to `<div>` causes Playwright e2e `page.getByRole("heading", { name: "Devices" })` to fail (proven by Playwright e2e failure in `device-config-ceremony.spec.ts:66`).
  2. Omitting posture badge or `re-export needed` on active posture-blocked device causes `deviceswiring.test.tsx` line 129 to fail red.
  3. Rendering posture badge on revoked device causes `deviceswiring.test.tsx` line 136 to fail red.
- **Exact-Count Census Edit:**
  - Adding a `pending` or OpenVPN device to `DEVICES` mock array in `deviceswiring.test.tsx` increases total row count from 3 to 4, requiring a deliberate edit to `deviceswiring.test.tsx:157` (`toHaveLength(4)`).

---

## 8. Fixture Reachability (Human Gate Limit Law)

- **Current Gap:** Standard `fixtures.sql` contains active/revoked devices but lacks `pending` status devices, `needs_reexport = true` static devices, and OpenVPN-transport devices (`public_key IS NULL`).
- **Fixture Expansion (`fixtures.sql`):**
  1. `unapproved-phone`: `status = 'pending'`, `assigned_ip = '10.99.0.15'`.
  2. `stale-laptop`: `status = 'active'`, `needs_reexport = true`, `assigned_ip = '10.99.0.16'`.
  3. `ovpn-contractor`: `status = 'active'`, `public_key = NULL` (OpenVPN profile), `assigned_ip = '10.99.0.17'`.

---

## 9. Navigation Audit Bucket & Endpoint Verification

- **Nav Audit Bucket:** `ACCESS` (under `NAV_GROUPS` in `AppShell.tsx`).
- **Verified Endpoint Calls:**
  - `GET /api/v1/organizations`
  - `GET /api/v1/organizations/{orgId}/devices`
  - `GET /api/v1/organizations/{orgId}/nodes`
  - `POST /api/v1/organizations/{orgId}/devices`
  - `POST /api/v1/organizations/{orgId}/ovpn-profiles`
  - `POST /api/v1/organizations/{orgId}/devices/{deviceId}/revoke`
  - All 6 endpoints belong to organization resource & device access management under `/api/v1/organizations/{orgId}/...`.
